--liquibase formatted sql
--changeset huangligui:curdie-2 dbms:h2

CREATE TABLE T_CURDIE_USER
(
   ID                   VARCHAR2(32)         NOT NULL,
   NAME                 VARCHAR2(32),
   SEX                  VARCHAR2(32),
   CREATE_TIME          TIMESTAMP,
   CREATE_USER          VARCHAR2(32),
   UPDATE_TIME          TIMESTAMP,
   UPDATE_USER          VARCHAR2(32),
   LAST_LOGIN_TIME      TIMESTAMP,
   IS_ONLINE            CHAR(1),
   CONSTRAINT PK_T_CURDIE_USER PRIMARY KEY (ID)
);

COMMENT ON COLUMN T_CURDIE_USER.NAME IS
'名称';

COMMENT ON COLUMN T_CURDIE_USER.SEX IS
'性别';

COMMENT ON COLUMN T_CURDIE_USER.CREATE_TIME IS
'创建时间';

COMMENT ON COLUMN T_CURDIE_USER.CREATE_USER IS
'创建人';

COMMENT ON COLUMN T_CURDIE_USER.UPDATE_TIME IS
'更新时间';

COMMENT ON COLUMN T_CURDIE_USER.UPDATE_USER IS
'更新人';